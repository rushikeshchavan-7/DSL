{
  "buildCommand": "cd studio && npm install && npm run build",
  "outputDirectory": "studio/dist/studio/browser",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
